import { plumise, addresses } from '@plumise/core'

export { plumise }

export const CONTRACTS = {
  REWARD_POOL: addresses.mainnet.RewardPool,
  AGENT_REGISTRY: addresses.mainnet.AgentRegistry,
  CHALLENGE_MANAGER: addresses.mainnet.ChallengeManager,
}
